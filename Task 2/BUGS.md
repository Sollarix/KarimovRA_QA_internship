# BUGS.md  
Баг-репорт по результатам автоматизированного тестирования API  
**Базовый URL**: `https://qa-internship.avito.com`  
**sellerID**: `989697` (уникальный)

---

## 🔴 Critical

### BUG-CR-001: `GET /api/1/item/:id` возвращает `[{}]`, а не `{}`  
> *Не упал в автотестах, но подтверждён вручную — оставлен для полноты.*

---

## 🟠 High

### BUG-H-001: `sellerID` вне диапазона `[111111, 999999]` принимается  
**Эндпоинт**: `POST /api/1/item`  
**Примеры**:  
- `sellerID: 111110` → `200 OK`  
- `sellerID: 1000000` → `200 OK`  

**Ожидаемый результат**: `400 Bad Request`, `"sellerID должно быть в диапазоне [111111, 999999]"`  
**Почему High**: Возможна подделка идентификаторов, нарушение целостности данных.

---

### BUG-H-002: Неконсистентность статус-кода при удалении несуществующего объявления  
**Эндпоинт**: `DELETE /api/2/item/:id`  
**Шаги**:  
1. Удалить несуществующее объявление (например, `ffffffff-ffff-ffff-ffff-ffffffffffff`)  

**Факт**:  
```http
404 Not Found
{ "result": { "message": "", "messages": null }, "status": "500" }
```

**Ожидаемо:**
```json
{ "status": "404", "result": { "message": "item <id> not found" } }
```

**Почему High:** Ложные алерты 5xx в мониторинге, отладка занимает время.

## 🟡 Medium
### BUG-M-001:** Имя из пробелов и спецсимволов принимается
**Эндпоинт:** POST /api/1/item
**Примеры:**
* name: " " → 200 OK
* name: "@ &# " → 200 OK

**Ожидаемый результат:** 400 Bad Request, "поле name не должно быть пустым / содержать только спецсимволы"
**Почему Medium:** На сайте появятся карточки с « » или «@ &#» — подрывает доверие.

### BUG-M-002: URL-encoding в сообщениях об ошибках (риск XSS)
**Эндпоинт:** GET /api/1/item/fb32а45f-b394-402а-ас54-е1е62bb9987!
**Факт:**

```json
{ "result": { "message": "ID айтема не UUID: fb32%D0%B045f-b394-402%D0%B0-%D0%B0%D1%8154-%D0%B51%D0%B562bb9987!" }, "status": "400" }
```

**Ожидаемо:**
```json
"ID айтема не UUID: fb32а45f-b394-402а-ас54-е1е62bb9987!" (UTF-8, без %D0%B0)
```

**Почему Medium:** снижает читаемость, а при рендеринге во фронте без декодирования — риск XSS.